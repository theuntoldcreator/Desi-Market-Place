// WARNING: These rules are for debugging purposes only and are not secure.
// They allow any authenticated user to read and write to the entire database.
// We will replace these with secure rules once the connection is confirmed to be working.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}