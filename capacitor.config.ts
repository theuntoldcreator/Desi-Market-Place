import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.desimarketplace',
  appName: 'Desi Market Place',
  webDir: 'dist'
};

export default config;
